# =====================================================
# MedAI - Frontend Environment Variables
# =====================================================
# Copy this file to .env.local and fill in your actual values
# NEVER commit .env.local to version control
# =====================================================

# =====================================================
# SUPABASE CONFIGURATION (Required)
# =====================================================
# Get these from: https://app.supabase.com/project/_/settings/api

VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# =====================================================
# BACKEND API (Optional - if using separate backend)
# =====================================================
VITE_API_BASE_URL=http://localhost:3000/api
VITE_SEARCH_SERVICE_URL=http://localhost:5000

# =====================================================
# GOOGLE MAPS API (Required for Location Features)
# =====================================================
# Get from: https://console.cloud.google.com/apis/credentials
# Enable: Maps JavaScript API, Places API, Geocoding API
VITE_GOOGLE_MAPS_API_KEY=your-google-maps-api-key

# =====================================================
# ENCRYPTION & SECURITY (Critical!)
# =====================================================
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"

VITE_ENCRYPTION_MASTER_KEY=generate-with-node-crypto-randomBytes-64
VITE_AADHAAR_HMAC_SECRET=generate-with-node-crypto-randomBytes-64
VITE_JWT_SECRET=generate-with-node-crypto-randomBytes-64
VITE_JWT_REFRESH_SECRET=generate-with-node-crypto-randomBytes-64

# JWT Token Expiry (in seconds)
VITE_JWT_ACCESS_TOKEN_EXPIRY=900
VITE_JWT_REFRESH_TOKEN_EXPIRY=604800

# =====================================================
# NDHM/ABDM INTEGRATION (Optional)
# =====================================================
# SANDBOX: https://sandbox.abdm.gov.in/
# Get credentials after formal onboarding
VITE_NDHM_BASE_URL=https://dev.ndhm.gov.in
VITE_NDHM_CLIENT_ID=your-ndhm-client-id
VITE_NDHM_CLIENT_SECRET=your-ndhm-client-secret

# =====================================================
# APPLICATION CONFIGURATION
# =====================================================
VITE_APP_NAME=MedAI
VITE_APP_VERSION=1.0.0
VITE_APP_URL=http://localhost:5173
VITE_APP_ENVIRONMENT=development

# =====================================================
# FEATURE FLAGS
# =====================================================
VITE_ENABLE_PWA=true
VITE_ENABLE_OFFLINE_MODE=true
VITE_ENABLE_ANALYTICS=false
VITE_ENABLE_ABDM=false

# =====================================================
# MONITORING & LOGGING
# =====================================================
# Sentry (Error tracking)
VITE_SENTRY_DSN=your-sentry-dsn
VITE_SENTRY_ENVIRONMENT=development

# Google Analytics
VITE_GA_TRACKING_ID=UA-XXXXXXXXX-X

# Log level: debug | info | warn | error
VITE_LOG_LEVEL=info

# =====================================================
# FILE UPLOAD CONFIGURATION
# =====================================================
VITE_MAX_FILE_SIZE=10485760
VITE_ALLOWED_FILE_TYPES=image/jpeg,image/png,application/pdf

# =====================================================
# LOCALIZATION
# =====================================================
VITE_DEFAULT_LANGUAGE=en
VITE_SUPPORTED_LANGUAGES=en,hi,ta,te,bn,mr,gu
VITE_EMAIL_API_KEY=your-email-api-key
VITE_EMAIL_FROM=noreply@medaidsathi.com

# Twilio / AWS SNS for SMS
VITE_SMS_API_KEY=your-sms-api-key
VITE_SMS_FROM=+91XXXXXXXXXX

# =====================================================
# FILE UPLOAD & STORAGE
# =====================================================

# Max file size in MB
VITE_MAX_FILE_SIZE_MB=10

# Allowed file types (comma-separated)
VITE_ALLOWED_FILE_TYPES=image/jpeg,image/png,application/pdf

# Malware scanning API (VirusTotal, ClamAV, etc.)
VITE_MALWARE_SCAN_API_KEY=your-virustotal-api-key

# =====================================================
# COMPLIANCE & AUDIT
# =====================================================

# Data retention period (in days)
VITE_DATA_RETENTION_DAYS=2555
VITE_AUDIT_LOG_RETENTION_DAYS=3650

# Consent version (increment on policy changes)
VITE_CONSENT_VERSION=1.0.0

# GDPR/DPDP Compliance flags
VITE_ENABLE_DATA_PORTABILITY=true
VITE_ENABLE_RIGHT_TO_ERASURE=true

# =====================================================
# DEPLOYMENT ENVIRONMENT
# =====================================================

# development | staging | production
NODE_ENV=development

# App version (for cache busting and rollback)
VITE_APP_VERSION=2.0.0

# Enable debug logging (only for development)
VITE_DEBUG=true

# =====================================================
# RATE LIMITING
# =====================================================

# Requests per minute per IP
VITE_RATE_LIMIT_RPM=60

# Max login attempts before lockout
VITE_MAX_LOGIN_ATTEMPTS=5

# Lockout duration (in minutes)
VITE_LOCKOUT_DURATION_MINUTES=15

# =====================================================
# SETUP INSTRUCTIONS
# =====================================================
# 1. Copy this file: cp .env.example .env
# 2. Fill in your actual values (from Supabase, Google Cloud, etc.)
# 3. NEVER commit .env to git
# 4. Rotate all secrets if previously exposed
# =====================================================

# =====================================================
# SECURITY CHECKLIST BEFORE DEPLOYMENT
# =====================================================
# [ ] All dummy values replaced with real keys
# [ ] .env.local added to .gitignore (already done)
# [ ] Service role key never exposed to frontend
# [ ] Google Maps API restricted by domain
# [ ] HMAC secrets minimum 64 characters
# [ ] Supabase RLS enabled on all tables
# [ ] All secrets rotated if previously exposed
# [ ] JWT expiry set to ≤15 minutes for access tokens
# [ ] NODE_ENV set to "production" in production
# [ ] VITE_DEBUG set to false in production
# [ ] Rate limiting configured
# [ ] Malware scanning enabled for file uploads
# [ ] Sentry configured for error tracking
# [ ] Audit logs enabled and monitored
# [ ] Data retention policies implemented
# =====================================================
